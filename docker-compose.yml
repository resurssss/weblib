services:
  backend:
    build:
      context: ./backend
    container_name: electolibrary-backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
      - ./backend/uploads:/app/uploads
    environment:
      - DATABASE_URL=sqlite:////data/tasks.db